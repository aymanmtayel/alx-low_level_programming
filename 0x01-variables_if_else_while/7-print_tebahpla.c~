#include <stdio.h>
/**
 *main - produce the alphabet in lower case then captalized
 *
 *Return: Always  0
 */
int main(void)
{
char l;

for (l = 'a' ; l <= 'z' ; --l)
putchar(l);
putchar('\n');

return (0);
}
